server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: airbnb-whatsapp
  datasource:
    url: jdbc:postgresql://localhost:5432/airbnbdb
    username: airbnb
    password: airbnb
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    out-of-order: false
    baseline-on-migrate: false
  thymeleaf:
    cache: true
    encoding: UTF-8


logging:
  level:
    root: INFO
    com.acme.airbnbwhatsapp: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized


evolution:
  base-url: http://localhost:8081
  api-key: "${EVOLUTION_API_KEY:airbnb-bot-123}"
  endpoints:
    send-text: /messages/text
    send-image: /messages/image
    send-document: /messages/document
    send-location: /messages/location

